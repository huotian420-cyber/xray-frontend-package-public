Windows package snapshot collected on 2026-04-27 from commit `bd9218a7`.

Includes:
- secure subscription conversion update in `SubscriptionHandler`
- bundled portable `v2rayN-windows-64.zip` package

Note:
- the portable zip is included in git
- the standalone installer exe is larger than the standard GitHub file size limit and is not mirrored here
